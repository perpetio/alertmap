package com.perpetio.alertapp.data

object Const {
    object NotificationChannel {
        val id = 1120
        val name = "AlertApp notification channel"
        val description = "This channel is used for sending notifications"
    }
}